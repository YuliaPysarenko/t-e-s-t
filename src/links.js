export default function linksDokQuerySel() {
    return {
        breadSelect: document.querySelector(`.breed-select`),
        error: document.querySelector(`.error`),
        loader: document.querySelector(`.loader`),
        catInfo: document.querySelector(`.cat-info`),
    };
}
